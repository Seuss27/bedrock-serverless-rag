## What and why

<!-- What changed, and why. Link the finding/issue if there is one. -->

## Sprint / Finding

Sprint: S<N>
Finding: F<N>

## Green gate

<!-- Paste the result of gates.green (.ai/project.yml) — tofu fmt, tofu validate, etc. -->

## Blast radius

- [ ] This PR does / does not change what CI may do in AWS (delete the one that doesn't apply)

## Plan summary

<!-- Only if a .tf file changed. Change counts + resource addresses ONLY — never raw
     `tofu plan` output (BR-D4): this repo is public and a plan renders the account id,
     role ARNs, bucket names, and the AOSS collection endpoint at runtime. -->
